.venv/
__pycache__/
*.pyc
*.egg-info/
.env
.DS_Store
_pdf_text_dump.json
# Ignore generated outputs, but keep one committed sample extraction so the repo
# is inspectable without running the pipeline.
out/*
!out/extraction.json
